Each tester object has a Test Script attached which, on Play, generates Repetitions number of integers between Min and Max and displays a graph of the resultant frequencies.

The Update button will cause the script to regenerate that graph every frame.

